# --- Web Framework ---
fastapi
uvicorn[standard]
python-dotenv

# --- LangChain & LLM (pinned to 0.3.x for langchain-google-genai compatibility) ---
langchain>=0.3.0,<1.0.0
langchain-core>=0.3.37,<0.4.0
langchain-google-genai>=2.0.0,<3.0.0
langchain-huggingface>=0.1.0,<1.0.0
langchain-community>=0.3.0,<1.0.0

# --- Embeddings & Vector Store ---
faiss-cpu
sentence-transformers

# --- Document Loading ---
pypdf

# --- CPU-only PyTorch (avoids 2GB+ CUDA packages that cause disk space errors on Choreo) ---
--extra-index-url https://download.pytorch.org/whl/cpu
torch
transformers
